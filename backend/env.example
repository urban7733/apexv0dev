# APEX VERIFY AI Backend Environment Configuration

# Environment
ENVIRONMENT=development

# API Configuration
API_V1_STR=/api
PROJECT_NAME=APEX VERIFY AI
VERSION=1.0.0

# Security
CORS_ORIGINS=["http://localhost:3000", "https://yourdomain.com"]

# File Upload
MAX_FILE_SIZE=104857600  # 100MB in bytes
ALLOWED_IMAGE_TYPES=["image/jpeg", "image/png", "image/webp", "image/gif"]

# AI Services
GEMINI_API_KEY=your_gemini_api_key_here
DINOV3_MODEL_PATH=/path/to/dinov3/model

# Database (for future use)
DATABASE_URL=postgresql://user:password@localhost:5432/apexverify

# Redis (for future use)
REDIS_URL=redis://localhost:6379

# Storage (for future use)
STORAGE_BUCKET=your-storage-bucket-name

# Logging
LOG_LEVEL=INFO

# Performance
MAX_CONCURRENT_VERIFICATIONS=10
