// Export authOptions for compatibility
export const authOptions = {
  // This is a placeholder for compatibility
  // The actual auth logic is handled in the AuthContext
  providers: [],
  callbacks: {},
  pages: {
    signIn: "/auth/signin",
  },
}
